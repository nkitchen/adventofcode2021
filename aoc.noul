adjs4 := \i, j -> [
    V(i - 1, j),
    V(i + 1, j),
    V(i, j - 1),
    V(i, j + 1)
];

adjs4_bounded := \m, n -> (
    \i, j -> (
        adjs4(i, j) filter (\[i', j'] -> 0 <= i' < m and 0 <= j' < n)
    )
)
